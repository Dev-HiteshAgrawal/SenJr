{
  "name": "senjr-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "firebase-functions": "^6.0.1",
    "livekit-server-sdk": "^2.15.2"
  }
}
